using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace MainApp.Tasks
{
    static class CommonElements
    {
        #region Task
        /*
         * Даны коллекции строк
         * Задача: вывести общие элементы, т.е которые содержатся и в первой коллекции и во второй.
         * На выход должны получить коллекцию общих элементов
         */
        #endregion
        public static IEnumerable<T> GetCommonElements<T>(IEnumerable<T> firstList, IEnumerable<T> secondList)
        {
            return default;
        }
    }
}
